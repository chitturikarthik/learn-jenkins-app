pipeline {
    agent any

    stages {
        stage('no docker') {
            steps {
               sh '''
                    echo "hello chaitanya"
                '''
            }
        }
        
    }
}
